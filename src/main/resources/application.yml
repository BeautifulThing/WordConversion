server:
  port: 8099
  servlet:
    context-path: /
logging:
  config: classpath:logback.xml
  file:
    name: word_conversion
spring:
  mvc:
    static-path-pattern: /**

app:
  domain: .*\.(wkinfo|wkpro)\.com\.cn
  name: WordChange
word:
  base:
    path: E\:/share/resources
  logo:
    name: logo_taa.png